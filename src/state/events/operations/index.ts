import fetchNotModeratedEvents from "./fetchNotModeratedEvents";
import acceptEvent from "./acceptEvent";

const eventsOperations = [
    fetchNotModeratedEvents,
    acceptEvent

];

export default eventsOperations;
